# Software

The software on the rover is split into multiple semi-independent "major projects", each of which contains several sub-projects.

## Core

The first (and largest!) is the main codebase which runs everything.
This is mostly ROS2 code, and is located in `software/ros_ws/src`.
