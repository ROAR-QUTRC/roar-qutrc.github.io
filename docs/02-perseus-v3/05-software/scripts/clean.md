# clean.bash

## Motivation

This script 'resets' the repo to the original state - removing all build
artifacts and logs. It should be updated with every new build system - for
example adding node_modules for a javascript package such as the webui.
